[package]
name = "app-ui"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[features]
test-support = ["gpui-kit/test-support", "overlay-win32/test-support"]

[dependencies]
app-core.workspace = true
gpui-kit.workspace = true
overlay-win32 = { path = "../overlay-win32" }
raw-window-handle = "=0.6.2"

[dev-dependencies]
overlay-win32 = { path = "../overlay-win32", features = ["test-support"] }
gpui-kit = { workspace = true, features = ["test-support"] }

[lints]
workspace = true
