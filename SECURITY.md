# Security policy

Do not open public issues containing credentials, private user data, or a
working exploit. Report vulnerabilities privately to the repository owner and
include affected versions, reproduction conditions, impact, and a proposed
mitigation when available.

The template does not persist user data or perform network requests. Product
code that adds those capabilities must document data classification, storage,
redaction, timeout, retry, and shutdown behavior.
